cookie:
  expiry_days: 30
  key: jain_proj_kecred # Must be a string
  name: auth
credentials:
  usernames:
    admin:
      email: admin@example.com
      failed_login_attempts: 0 # Will be managed automatically
      logged_in: False # Will be managed automatically
      password: abc # Will be hashed automatically
      first_name: "Admin"
      last_name: "User"
      